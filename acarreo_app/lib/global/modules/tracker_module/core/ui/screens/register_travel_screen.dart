import 'package:flutter/widgets.dart';

class RegisterTravelScreen extends StatelessWidget {
  const RegisterTravelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
