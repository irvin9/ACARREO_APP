<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ACARREO_APP"
        tools:targetApi="31">
        <activity
            android:name=".ACARREO_FILE.nfcActivity"
            android:exported="false" />
        <activity
            android:name=".ACARREO_FILE.ScanActivity"
            android:exported="false" />
        <activity
            android:name=".ACARREO_FILE.ThirdActivity"
            android:exported="false" />
        <activity
            android:name=".ACARREO_FILE.SecondActivity"
            android:exported="false" />
        <activity
            android:name=".ACARREO_FILE.FirstAppActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!--NCF Related-->
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>